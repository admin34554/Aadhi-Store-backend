spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/aadhi_store
    username: postgres
    password: 1121
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        default_schema: tenant_default

server:
  port: 9090
